# Final System: MPC 1.2.1
# This section is done in Chroot environment

# Configure source
./configure --prefix=/usr         \
            --disable-static      \
            --docdir=/usr/share/doc/mpc-1.2.1

# Build
make

# Install
make install
