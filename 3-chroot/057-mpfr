# Final System: MPFR 4.1.0
# Souce: http://www.mpfr.org/mpfr-4.1.0/mpfr-4.1.0.tar.xz
# // Part of the mini GCC-toolchain for packages that will not build with LLVM:
# // /opt/gnu
# This section is done in Chroot environment

# Configure source
CC=clang CXX=clang++             \
./configure --prefix=/opt/gnu    \
            --disable-static     \
            --enable-thread-safe \
            --with-gmp=/opt/gnu/ \
            --docdir=/opt/gnu/share/doc/mpfr-4.1.0

# Build
make

# Install
make install
