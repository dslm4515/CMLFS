# Final System: Wheel (python module)
# Build and install under chroot

# Compile with pip
pip3 wheel -w dist --no-build-isolation --no-deps $PWD

# Install
pip3 install --no-index --find-links=dist wheel
