# Final System: Expat
# This section is done in Chroot environment

CC=clang CXX=clang++         \
./configure --prefix=/usr    \
            --disable-static \
            --docdir=/usr/share/doc/expat-2.5.0
make && make install
