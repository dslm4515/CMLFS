# Final System: Skalibs
# This section is done in Chroot environment

# Configure source
./configure --prefix=/usr       \
            --build=${TUPLE}   \
            --enable-static     \
            --enable-shared     \
            --libdir=/usr/lib

# Build and Install
make
make install
