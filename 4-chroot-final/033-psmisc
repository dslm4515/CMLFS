# Final System: Psmisc
# This section is done in Chroot environment

CC=clang CXX=clang++ \
./configure --prefix=/usr        \
            --sysconfdir=/etc     \
            --localstatedir=/var  \
            --disable-selinux     \
            --enable-harden-flags \
            --enable-ipv6

make && make install
