# Final System: M4
# Build under chroot

# Configure
ac_cv_lib_error_at_line=no \
ac_cv_header_sys_cdefs_h=no \
./configure --prefix=/usr \
            --enable-changeword \
            --enable-threads

# Build and install
make && make install
