# llvmtools: heirloom-ng
# Build as cmlfs
#
# NOTE: Replaces coreutils, diffutils, findutils, grep, sed

# Fix an issue with nawk source
patch -Np1 -i ../patches/heirloom-ng-cmlfs/fix_nawk_main_args.patch

# Use a config file with correct values
mv -v build/mk.config{,.orig}
cp -v ../files/heirloom-ng/mk.config build/

# compile
make

# install
make install

# Make links for 'missing' binary
ln -s ../ucbbin/install /llvmtools/bin/install
