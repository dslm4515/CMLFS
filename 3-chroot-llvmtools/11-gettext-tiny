# llvmtools: Gettext-tiny
# Build under chroot

# Build
make LIBINTL=MUSL prefix=/llvmtools

# Fix then install the msgfmt, msgmerge and xgettext programs to toolchain
for b in autopoint msgfmt msgmerge xgettext
do
   setDL $b
   cp -v $b /llvmtools/bin/
done
