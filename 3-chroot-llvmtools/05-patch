# llvmtools: Patch
# Build and install under chroot

# Configure will expect binutils. Create links
# to force configure to use LLVM's binutils
set-llvm-bin-mode

# Configure source
./configure --prefix=/llvmtools

# Build and install to toolchain
make && make install

# Set the dynamic loader for each executable
setDL /llvmtools/bin/patch
